pipeline {
  agent {
    docker {image 'node:16-alpine'}  
   }
   stages {
      stage('Test') {
        steps {
          sh 'node --version'
        }
      }
   stage('command') {
       steps {
          sh 'echo "Hello Jenkins'
          }
        }
     stage('print') {
       steps {
          sh 'echo "node is installed"'
          }
        }
   }
}
