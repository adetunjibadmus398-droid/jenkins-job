pipeline {
  agent {
    docker {image 'node:16-alpine'}  
   }
   stages {
      stage('Test') {
        steps {
          sh 'node --version'
        }
      }
   stage('command') {
       steps {
          sh 'echo "Hello Jenkins from Stranger things'
          }
        }
     stage('print') {
       steps {
          sh 'echo "node is installed"'
          }
        }
   }
}
